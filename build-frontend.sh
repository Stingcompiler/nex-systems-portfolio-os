#!/bin/bash
set -e

# البناء بلا API إطلاقًا (NEXT_BUILD_OFFLINE): كل جلب يعود بقيمته الفارغة
# فورًا دون أن يُخزَّن شيء في ذاكرة الجلب. البديل السابق — تشغيل Django على
# قاعدة فارغة — كان يخبز قوائم فارغة في ذاكرة الجلب مع صلاحية 10 دقائق،
# فتُقدَّم بعد النشر صفحات بلا خدمات ولا مشاريع حتى تنتهي دورتان من إعادة
# التحقق. الصفحات المولَّدة هنا فارغة أيضًا لكنها تُبطَل عند الإقلاع (start.sh).
cd /app/frontend
NEXT_BUILD_OFFLINE=true npm run build
